import 'package:flutter/material.dart';

class PriceSilderWidget extends StatelessWidget
{
  const PriceSilderWidget({super.key});

  @override
  Widget build(BuildContext context)
  {
    return Container();
  }
}
