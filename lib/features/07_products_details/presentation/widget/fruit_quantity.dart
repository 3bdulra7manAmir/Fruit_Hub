import 'package:flutter/widgets.dart';

class FruitNeededQuantity extends StatelessWidget
{
  const FruitNeededQuantity({super.key});

  @override
  Widget build(BuildContext context)
  {
    return Container();
  }
}